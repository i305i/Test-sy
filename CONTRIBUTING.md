# 🤝 دليل المساهمة (Contributing Guide)

مرحباً بك في مشروع نظام إدارة الشركات والوثائق! نحن سعداء بمساهمتك.

---

## 📋 جدول المحتويات

1. [مدونة السلوك](#-مدونة-السلوك)
2. [كيف يمكنني المساهمة؟](#-كيف-يمكنني-المساهمة)
3. [إعداد بيئة التطوير](#️-إعداد-بيئة-التطوير)
4. [معايير التطوير](#-معايير-التطوير)
5. [عملية المساهمة](#-عملية-المساهمة)
6. [الإبلاغ عن الأخطاء](#-الإبلاغ-عن-الأخطاء)
7. [طلب ميزات جديدة](#-طلب-ميزات-جديدة)
8. [مراجعة الكود](#-مراجعة-الكود)

---

## 📜 مدونة السلوك

### تعهدنا

نحن كمساهمين ومشرفين نتعهد بجعل المساهمة في مشروعنا تجربة خالية من المضايقات للجميع.

### معاييرنا

**سلوكيات إيجابية:**
- ✅ استخدام لغة ترحيبية وشاملة
- ✅ احترام وجهات النظر والتجارب المختلفة
- ✅ قبول النقد البنّاء بلطف
- ✅ التركيز على ما هو أفضل للمجتمع
- ✅ إظهار التعاطف مع أعضاء المجتمع الآخرين

**سلوكيات غير مقبولة:**
- ❌ استخدام لغة أو صور جنسية
- ❌ التعليقات المسيئة أو الشخصية
- ❌ المضايقة العامة أو الخاصة
- ❌ نشر معلومات خاصة للآخرين
- ❌ أي سلوك غير مهني

### المسؤوليات

مشرفو المشروع مسؤولون عن توضيح معايير السلوك المقبول وسيتخذون إجراءات تصحيحية عادلة ومناسبة.

---

## 💡 كيف يمكنني المساهمة؟

هناك عدة طرق للمساهمة:

### 1. الإبلاغ عن الأخطاء 🐛
ساعدنا في تحسين النظام بالإبلاغ عن الأخطاء التي تجدها.

### 2. اقتراح ميزات جديدة 💡
لديك فكرة رائعة؟ شاركها معنا!

### 3. كتابة الكود 💻
ساهم في تطوير الميزات أو إصلاح الأخطاء.

### 4. تحسين التوثيق 📚
التوثيق الجيد مهم جداً، ساعدنا في تحسينه.

### 5. ترجمة 🌍
ساعد في ترجمة النظام للغات أخرى.

### 6. المراجعة والاختبار 🧪
راجع Pull Requests واختبر الميزات الجديدة.

---

## 🛠️ إعداد بيئة التطوير

### المتطلبات الأساسية

```bash
# التأكد من النسخ المطلوبة
node --version  # v20.x أو أحدث
npm --version   # أحدث إصدار
docker --version  # v24.x أو أحدث
git --version   # أحدث إصدار
```

### خطوات الإعداد

#### 1. Fork المشروع
1. اذهب إلى صفحة المشروع على GitHub
2. اضغط على "Fork" في الأعلى
3. سيتم إنشاء نسخة في حسابك

#### 2. Clone المشروع

```bash
# استنساخ fork الخاص بك
git clone https://github.com/YOUR_USERNAME/company-docs-manager.git
cd company-docs-manager

# إضافة remote للمشروع الأصلي
git remote add upstream https://github.com/ORIGINAL_OWNER/company-docs-manager.git

# التحقق من remotes
git remote -v
```

#### 3. إعداد البيئة

```bash
# نسخ ملف البيئة
cp env.example.txt .env

# تعديل المتغيرات حسب الحاجة
nano .env
```

#### 4. تشغيل الخدمات

```bash
# تشغيل باستخدام Docker Compose
docker-compose --profile dev up -d

# أو تشغيل بدون Docker
# Backend
cd backend
npm install
npm run migrate
npm run seed
npm run dev

# Frontend (في terminal آخر)
cd frontend
npm install
npm run dev
```

#### 5. التحقق من التشغيل

- Frontend: http://localhost:3000
- Backend API: http://localhost:5000/api/v1
- API Docs: http://localhost:5000/api-docs

---

## 🎨 معايير التطوير

### بنية الكود

#### Frontend (React/Next.js)

```typescript
// ✅ جيد: مكون وظيفي مع TypeScript
interface CompanyCardProps {
  company: Company;
  onEdit: (id: string) => void;
}

export const CompanyCard: React.FC<CompanyCardProps> = ({ company, onEdit }) => {
  return (
    <div className="card">
      <h3>{company.name}</h3>
      <button onClick={() => onEdit(company.id)}>تعديل</button>
    </div>
  );
};

// ❌ سيء: بدون types، props غير واضحة
export const CompanyCard = (props) => {
  return <div>{props.company.name}</div>;
};
```

#### Backend (Node.js/Express)

```typescript
// ✅ جيد: Controller منظم مع error handling
export class CompaniesController {
  async getCompany(req: Request, res: Response, next: NextFunction) {
    try {
      const { id } = req.params;
      
      const company = await CompaniesService.getById(id);
      
      if (!company) {
        throw new NotFoundError('الشركة غير موجودة');
      }
      
      return res.json({
        success: true,
        data: { company }
      });
    } catch (error) {
      next(error);
    }
  }
}

// ❌ سيء: بدون error handling، كود غير منظم
app.get('/company/:id', (req, res) => {
  const company = db.query('SELECT * FROM companies WHERE id = ?', req.params.id);
  res.json(company);
});
```

### التسميات (Naming Conventions)

#### متغيرات ووظائف
```typescript
// ✅ جيد: camelCase للمتغيرات والوظائف
const userName = 'أحمد';
const userEmail = 'ahmed@example.com';

function getUserById(id: string) {
  // ...
}

// ❌ سيء
const user_name = 'أحمد';
const UserEmail = 'ahmed@example.com';
```

#### Classes و Components
```typescript
// ✅ جيد: PascalCase للـ Classes و Components
class CompaniesService {
  // ...
}

const CompanyCard: React.FC = () => {
  // ...
};

// ❌ سيء
class companiesService {
  // ...
}
```

#### Constants
```typescript
// ✅ جيد: UPPER_SNAKE_CASE للثوابت
const MAX_FILE_SIZE = 50 * 1024 * 1024;
const API_BASE_URL = 'http://localhost:5000';

// ❌ سيء
const maxFileSize = 50 * 1024 * 1024;
```

#### Files و Directories
```typescript
// ✅ جيد: kebab-case للملفات
company-card.component.tsx
user-profile.service.ts
api-routes.ts

// ❌ سيء
CompanyCard.component.tsx
UserProfile_Service.ts
```

### Git Commit Messages

نستخدم [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# الصيغة
<type>(<scope>): <description>

[optional body]

[optional footer]
```

#### الأنواع (Types)
- **feat**: ميزة جديدة
- **fix**: إصلاح خطأ
- **docs**: تعديلات على التوثيق
- **style**: تغييرات في التنسيق (لا تؤثر على الكود)
- **refactor**: إعادة هيكلة الكود
- **test**: إضافة أو تعديل اختبارات
- **chore**: مهام صيانة

#### أمثلة

```bash
# ✅ جيد
feat(companies): إضافة ميزة أرشفة الشركات
fix(auth): إصلاح مشكلة انتهاء صلاحية token
docs(api): تحديث توثيق API الخاص بالمشاركة
refactor(documents): تحسين أداء رفع الملفات
test(users): إضافة اختبارات للمصادقة

# ❌ سيء
update code
fixed bug
changes
WIP
```

### التعليقات (Comments)

```typescript
// ✅ جيد: تعليقات توضح "لماذا"
// نستخدم exponential backoff للتعامل مع rate limiting
const retryWithBackoff = async (fn: Function, retries = 3) => {
  // ...
};

// ❌ سيء: تعليقات توضح "ماذا" (واضح من الكود)
// هذه الدالة تجمع رقمين
function add(a: number, b: number): number {
  return a + b;
}

// ✅ جيد: JSDoc للوظائف العامة
/**
 * يرفع وثيقة جديدة للشركة المحددة
 * @param companyId - معرف الشركة
 * @param file - ملف الوثيقة
 * @returns Promise يحتوي على بيانات الوثيقة المرفوعة
 * @throws {NotFoundError} إذا لم تكن الشركة موجودة
 * @throws {ValidationError} إذا كان نوع الملف غير مدعوم
 */
async function uploadDocument(companyId: string, file: File): Promise<Document> {
  // ...
}
```

---

## 🔄 عملية المساهمة

### 1. اختر Issue أو أنشئ واحد

```bash
# ابحث عن issue مع label "good first issue" أو "help wanted"
# أو أنشئ issue جديد لوصف ما تريد العمل عليه
```

### 2. أنشئ Branch جديد

```bash
# تأكد أنك على main محدث
git checkout main
git pull upstream main

# أنشئ branch جديد
git checkout -b feat/add-company-archiving

# تسميات الـ branches:
# feat/feature-name    - ميزة جديدة
# fix/bug-name         - إصلاح خطأ
# docs/topic           - تحديث توثيق
# refactor/component   - إعادة هيكلة
```

### 3. اكتب الكود

```typescript
// اتبع معايير الكود
// أضف اختبارات
// حدّث التوثيق إن لزم
```

### 4. اختبر التعديلات

```bash
# Backend tests
cd backend
npm test

# Frontend tests
cd frontend
npm test

# Linting
npm run lint

# Type checking
npm run type-check
```

### 5. Commit التعديلات

```bash
git add .
git commit -m "feat(companies): إضافة ميزة أرشفة الشركات

- إضافة endpoint للأرشفة
- إضافة واجهة المستخدم
- إضافة اختبارات
- تحديث التوثيق

Closes #123"
```

### 6. Push للـ Fork

```bash
git push origin feat/add-company-archiving
```

### 7. أنشئ Pull Request

1. اذهب إلى fork الخاص بك على GitHub
2. اضغط "New Pull Request"
3. اختر branch الخاص بك
4. املأ القالب:

```markdown
## الوصف
وصف مختصر للتعديلات

## نوع التعديل
- [ ] إصلاح خطأ (fix)
- [x] ميزة جديدة (feat)
- [ ] تحديث توثيق (docs)
- [ ] إعادة هيكلة (refactor)

## كيف تم الاختبار؟
- اختبارات وحدة
- اختبار يدوي
- ...

## Checklist
- [x] الكود يتبع معايير المشروع
- [x] تمت إضافة اختبارات
- [x] تم تحديث التوثيق
- [x] تم اختبار التعديلات

## لقطات الشاشة (إن وجدت)
```

---

## 🐛 الإبلاغ عن الأخطاء

### قبل الإبلاغ

1. **تأكد أنك تستخدم أحدث إصدار**
2. **ابحث في Issues الموجودة** - قد يكون شخص آخر أبلغ عن نفس المشكلة
3. **حاول إعادة إنتاج المشكلة** - تأكد أن المشكلة قابلة للتكرار

### قالب الإبلاغ

```markdown
## وصف المشكلة
وصف واضح ومختصر للمشكلة

## خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. اضغط على '...'
3. مرر إلى '...'
4. شاهد الخطأ

## السلوك المتوقع
ما كان يجب أن يحدث

## السلوك الفعلي
ما حدث فعلاً

## لقطات الشاشة
إن أمكن، أضف لقطات شاشة

## البيئة
- OS: [e.g. Windows 11]
- المتصفح: [e.g. Chrome 120]
- النسخة: [e.g. 1.0.0]

## معلومات إضافية
أي معلومات أخرى مفيدة

## السجلات (Logs)
```
نسخ السجلات هنا
```
```

---

## 💡 طلب ميزات جديدة

### قالب طلب الميزة

```markdown
## المشكلة أو الحاجة
وصف واضح للمشكلة التي تحلها هذه الميزة

## الحل المقترح
وصف الحل الذي تقترحه

## البدائل المدروسة
وصف أي بدائل أو حلول أخرى فكرت فيها

## الفوائد
- فائدة 1
- فائدة 2

## الأولوية
- [ ] منخفضة
- [ ] متوسطة
- [x] عالية

## معلومات إضافية
أي معلومات أخرى أو لقطات شاشة
```

---

## 👀 مراجعة الكود

### للمراجعين

عند مراجعة Pull Request:

#### ✅ تحقق من:
- [ ] الكود يتبع معايير المشروع
- [ ] الاختبارات موجودة وتعمل
- [ ] التوثيق محدّث
- [ ] لا توجد أخطاء linting
- [ ] الأداء مقبول
- [ ] الأمان مراعى
- [ ] لا توجد breaking changes غير موثقة

#### 💬 التعليقات

```markdown
# ✅ جيد: تعليق بناء
اقتراح: يمكن تحسين الأداء هنا باستخدام useMemo
```javascript
const filteredCompanies = useMemo(
  () => companies.filter(c => c.status === 'active'),
  [companies]
);
```

# ❌ سيء: تعليق غير بناء
هذا الكود سيء، أعد كتابته
```

#### التقييمات
- ✅ **Approve**: الكود جاهز للدمج
- 💬 **Comment**: تعليقات للنقاش
- ⚠️ **Request Changes**: تعديلات مطلوبة

---

## 🧪 الاختبارات

### كتابة الاختبارات

#### Unit Tests

```typescript
// companies.service.test.ts
describe('CompaniesService', () => {
  describe('getById', () => {
    it('يجب أن يعيد الشركة إذا كانت موجودة', async () => {
      const company = await CompaniesService.getById('valid-id');
      expect(company).toBeDefined();
      expect(company.id).toBe('valid-id');
    });

    it('يجب أن يرمي NotFoundError إذا لم تكن الشركة موجودة', async () => {
      await expect(
        CompaniesService.getById('invalid-id')
      ).rejects.toThrow(NotFoundError);
    });
  });
});
```

#### Integration Tests

```typescript
// companies.routes.test.ts
describe('GET /api/companies/:id', () => {
  it('يجب أن يعيد 200 وبيانات الشركة', async () => {
    const response = await request(app)
      .get('/api/companies/valid-id')
      .set('Authorization', `Bearer ${validToken}`);

    expect(response.status).toBe(200);
    expect(response.body.success).toBe(true);
    expect(response.body.data.company).toBeDefined();
  });

  it('يجب أن يعيد 404 إذا لم تكن الشركة موجودة', async () => {
    const response = await request(app)
      .get('/api/companies/invalid-id')
      .set('Authorization', `Bearer ${validToken}`);

    expect(response.status).toBe(404);
  });
});
```

---

## 📚 موارد إضافية

### الوثائق
- [README.md](README.md) - نظرة عامة على المشروع
- [API_DOCUMENTATION.md](API_DOCUMENTATION.md) - توثيق الـ API
- [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md) - مخطط قاعدة البيانات
- [SECURITY_GUIDE.md](SECURITY_GUIDE.md) - دليل الأمان

### الأدوات المساعدة
- [TypeScript](https://www.typescriptlang.org/)
- [React](https://react.dev/)
- [Next.js](https://nextjs.org/)
- [Node.js](https://nodejs.org/)
- [Express](https://expressjs.com/)
- [Prisma](https://www.prisma.io/)

### الدعم
- **GitHub Issues**: للأسئلة والمشاكل
- **Discord**: [رابط الخادم]
- **Email**: dev@companydocs.com

---

## 🎉 شكراً لمساهمتك!

كل مساهمة، مهما كانت صغيرة، تساعد في تحسين المشروع. نحن نقدر وقتك وجهدك!

**المساهمون الأوائل**:
- سيتم إضافة أسمائهم هنا

---

## 📜 الترخيص

بمساهمتك في هذا المشروع، فإنك توافق على أن مساهماتك ستكون مرخصة تحت نفس الترخيص.

