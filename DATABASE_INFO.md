# 📊 ملفات قاعدة البيانات

## 📁 الملفات الرئيسية:

### 1. ملف Schema (تعريف الجداول والعلاقات)
**المسار:** `backend/prisma/schema.prisma`

هذا الملف يحتوي على:
- تعريف جميع الجداول (Users, Companies, Documents, etc.)
- العلاقات بين الجداول
- الأنواع (Enums) المستخدمة

### 2. ملف البيانات الأولية (Seed Data)
**المسار:** `backend/prisma/seed.ts`

هذا الملف يحتوي على البيانات الأولية التي يتم إدخالها عند تشغيل `npm run seed`:
- مستخدم Super Admin: admin@companydocs.com / Admin@123
- مستخدم Supervisor: supervisor@companydocs.com / Supervisor@123
- مستخدم Employee: employee@companydocs.com / Employee@123
- شركة تجريبية: "شركة الأمل للتجارة"

### 3. ملفات Migrations (SQL)
**المسار:** `backend/prisma/migrations/`

تحتوي على ملفات SQL التي تنشئ الجداول:
- `20251106135240_init/migration.sql` - إنشاء الجداول الأساسية
- `20251107044239_add_folders_system/migration.sql` - إضافة نظام المجلدات
- `20251107053825_add_download_tokens/migration.sql` - إضافة نظام التحميل

---

## 💾 قاعدة البيانات الفعلية:

### إذا كنت تستخدم Docker:
قاعدة البيانات موجودة في Docker Volume:
```bash
docker volume ls | grep postgres
```

البيانات موجودة في: `/var/lib/postgresql/data` داخل الحاوية

### إذا كنت تستخدم PostgreSQL محلي:
قاعدة البيانات موجودة في:
- **Windows:** `C:\Program Files\PostgreSQL\16\data\`
- **Linux/Mac:** `/var/lib/postgresql/16/main/`

اسم قاعدة البيانات: `company_docs` (حسب ملف .env)

---

## 🔍 كيفية عرض البيانات:

### 1. استخدام Prisma Studio (واجهة رسومية)
```bash
cd backend
npm run studio
```
سيفتح المتصفح على: http://localhost:5555

### 2. استخدام psql (سطر الأوامر)
```bash
psql -U postgres -d company_docs
```

### 3. استخدام pgAdmin (إذا كان مثبتاً)
اتصل بـ: localhost:5432
Database: company_docs

---

## 📝 الجداول الموجودة:

1. **users** - المستخدمين
2. **companies** - الشركات
3. **documents** - الوثائق
4. **folders** - المجلدات
5. **company_shares** - المشاركات
6. **access_requests** - طلبات الوصول
7. **comments** - التعليقات
8. **notifications** - الإشعارات
9. **sessions** - الجلسات
10. **audit_logs** - سجلات التدقيق
11. **download_tokens** - رموز التحميل

---

## 🚀 كيفية إعادة تعبئة البيانات:

```bash
cd backend
npm run seed
```

هذا الأمر سيقوم بـ:
- إنشاء/تحديث المستخدمين الافتراضيين
- إنشاء شركة تجريبية
- إضافة بيانات أولية

