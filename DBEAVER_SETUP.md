# 🔌 إعداد قاعدة البيانات في DBeaver

## 📋 معلومات الاتصال

### إذا كنت تستخدم Docker:
- **Host:** `localhost`
- **Port:** `5432`
- **Database:** `company_docs`
- **Username:** `postgres`
- **Password:** `postgres123`

### إذا كنت تستخدم PostgreSQL محلي:
استخدم نفس المعلومات أعلاه (حسب ملف .env)

---

## 🚀 خطوات الإعداد في DBeaver

### الخطوة 1: إنشاء اتصال جديد

1. افتح **DBeaver**
2. اضغط على أيقونة **"New Database Connection"** (أو `Ctrl+Shift+N`)
3. اختر **PostgreSQL** من قائمة قواعد البيانات
4. اضغط **Next**

### الخطوة 2: إدخال معلومات الاتصال

في نافذة الإعدادات، أدخل:

```
Main Tab:
├── Host: localhost
├── Port: 5432
├── Database: company_docs
├── Username: postgres
└── Password: postgres123
```

**ملاحظة:** تأكد من تفعيل خيار **"Save password"** لحفظ كلمة المرور

### الخطوة 3: اختبار الاتصال

1. اضغط على زر **"Test Connection"**
2. إذا ظهرت رسالة "Connected" ✅، يعني الاتصال نجح
3. إذا ظهرت رسالة خطأ، تأكد من:
   - PostgreSQL يعمل
   - المنفذ 5432 مفتوح
   - كلمة المرور صحيحة

### الخطوة 4: حفظ الاتصال

1. اضغط **Finish** لحفظ الاتصال
2. سيظهر الاتصال في قائمة **Database Navigator** على اليسار

---

## 🔍 عرض البيانات

بعد الاتصال، يمكنك:

### 1. عرض الجداول:
- انقر نقراً مزدوجاً على قاعدة البيانات `company_docs`
- افتح مجلد **"Schemas"** → **"public"** → **"Tables"**
- ستجد جميع الجداول:
  - `users` - المستخدمين
  - `companies` - الشركات
  - `documents` - الوثائق
  - `folders` - المجلدات
  - وغيرها...

### 2. عرض البيانات:
- انقر بزر الماوس الأيمن على أي جدول
- اختر **"View Data"** أو **"Edit Data"**

### 3. تشغيل استعلامات SQL:
- اضغط `Ctrl+Alt+X` لفتح نافذة SQL Editor
- اكتب استعلامات SQL مثل:
```sql
SELECT * FROM users;
SELECT * FROM companies;
SELECT * FROM documents;
```

---

## 🛠️ حل المشاكل

### المشكلة: "Connection refused"
**الحل:**
- تأكد من تشغيل PostgreSQL
- إذا كنت تستخدم Docker:
  ```bash
  docker-compose -f docker-compose.simple.yml ps
  ```
- تحقق من أن المنفذ 5432 غير مستخدم

### المشكلة: "Authentication failed"
**الحل:**
- تحقق من كلمة المرور في ملف `.env`
- تأكد من أن المستخدم `postgres` موجود

### المشكلة: "Database does not exist"
**الحل:**
- قم بإنشاء قاعدة البيانات أولاً:
  ```bash
  cd backend
  npx prisma migrate dev
  ```

---

## 📊 استعلامات مفيدة

### عرض جميع المستخدمين:
```sql
SELECT id, email, first_name, last_name, role, status 
FROM users;
```

### عرض جميع الشركات:
```sql
SELECT id, name, name_arabic, company_type, status, created_at 
FROM companies;
```

### عرض الوثائق:
```sql
SELECT d.id, d.name, d.category, d.status, c.name as company_name
FROM documents d
JOIN companies c ON d.company_id = c.id;
```

### إحصائيات سريعة:
```sql
-- عدد المستخدمين
SELECT COUNT(*) as total_users FROM users;

-- عدد الشركات
SELECT COUNT(*) as total_companies FROM companies;

-- عدد الوثائق
SELECT COUNT(*) as total_documents FROM documents;

-- توزيع المستخدمين حسب الدور
SELECT role, COUNT(*) as count 
FROM users 
GROUP BY role;
```

---

## 💡 نصائح إضافية

1. **تصدير البيانات:**
   - انقر بزر الماوس الأيمن على الجدول
   - اختر **"Export Data"**
   - اختر الصيغة المطلوبة (CSV, Excel, JSON, etc.)

2. **استيراد البيانات:**
   - انقر بزر الماوس الأيمن على الجدول
   - اختر **"Import Data"**
   - اختر الملف المراد استيراده

3. **تصدير المخطط (Schema):**
   - انقر بزر الماوس الأيمن على قاعدة البيانات
   - اختر **"Export Schema"**

4. **نسخ احتياطي:**
   - انقر بزر الماوس الأيمن على قاعدة البيانات
   - اختر **"Backup Database"**

---

## 🔗 روابط مفيدة

- [DBeaver Documentation](https://dbeaver.com/docs/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

## ✅ Checklist

- [ ] DBeaver مثبت
- [ ] PostgreSQL يعمل
- [ ] قاعدة البيانات `company_docs` موجودة
- [ ] الاتصال نجح في DBeaver
- [ ] يمكن عرض الجداول
- [ ] يمكن تشغيل استعلامات SQL

