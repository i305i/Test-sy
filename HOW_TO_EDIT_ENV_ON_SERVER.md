# 📝 دليل تعديل ملف env على الخادم

## 🎯 الطرق المختلفة لتعديل ملف env

---

## الطريقة 1: استخدام SSH و nano/vim (الأسهل)

### خطوات الاتصال بالخادم:

```bash
# الاتصال بالخادم عبر SSH
ssh username@93.127.160.182

# أو إذا كان المنفذ مختلف
ssh -p 22 username@93.127.160.182
```

### تعديل ملف env للفرونت اند:

```bash
# الانتقال لمجلد المشروع
cd /path/to/your/project/frontend

# تعديل ملف env باستخدام nano (سهل للمبتدئين)
nano env

# أو باستخدام vim (للمحترفين)
vim env
```

### في nano:
- **عدّل** المحتوى كما تريد
- **احفظ**: اضغط `Ctrl + O` ثم `Enter`
- **اخرج**: اضغط `Ctrl + X`

### في vim:
- **دخول وضع التعديل**: اضغط `i`
- **عدّل** المحتوى
- **حفظ وخرج**: اضغط `Esc` ثم اكتب `:wq` واضغط `Enter`
- **خرج بدون حفظ**: اضغط `Esc` ثم اكتب `:q!` واضغط `Enter`

---

## الطريقة 2: استخدام SCP (نسخ من/إلى الخادم)

### من جهازك المحلي إلى الخادم:

```bash
# نسخ ملف env من جهازك إلى الخادم
scp frontend/env username@93.127.160.182:/path/to/project/frontend/env
```

### من الخادم إلى جهازك المحلي:

```bash
# نسخ ملف env من الخادم إلى جهازك
scp username@93.127.160.182:/path/to/project/frontend/env frontend/env
```

**مثال:**
```bash
# تعديل الملف محلياً
nano frontend/env

# نسخه للخادم
scp frontend/env root@93.127.160.182:/var/www/company-docs/frontend/env
```

---

## الطريقة 3: استخدام echo (لإضافة سطر واحد)

```bash
# الاتصال بالخادم
ssh username@93.127.160.182

# الانتقال للمجلد
cd /path/to/project/frontend

# إضافة أو تعديل سطر واحد
echo "NEXT_PUBLIC_API_URL=http://93.127.160.182:5000/api/v1" > env
echo "NEXT_PUBLIC_APP_NAME=نظام إدارة الشركات" >> env
echo "NEXT_PUBLIC_APP_VERSION=1.0.0" >> env
```

**ملاحظة:**
- `>` = استبدال الملف بالكامل
- `>>` = إضافة سطر جديد

---

## الطريقة 4: استخدام sed (لتعديل سطر محدد)

```bash
# الاتصال بالخادم
ssh username@93.127.160.182

cd /path/to/project/frontend

# تعديل سطر محدد (استبدال localhost بـ IP)
sed -i 's|http://localhost:5000/api/v1|http://93.127.160.182:5000/api/v1|g' env
```

---

## الطريقة 5: استخدام cat مع heredoc

```bash
ssh username@93.127.160.182
cd /path/to/project/frontend

# إنشاء ملف جديد
cat > env << 'EOF'
NEXT_PUBLIC_API_URL=http://93.127.160.182:5000/api/v1
NEXT_PUBLIC_APP_NAME=نظام إدارة الشركات
NEXT_PUBLIC_APP_VERSION=1.0.0
EOF
```

---

## 📋 مثال كامل: تعديل ملف env للفرونت اند

### الخطوة 1: الاتصال بالخادم
```bash
ssh root@93.127.160.182
```

### الخطوة 2: الانتقال لمجلد المشروع
```bash
# ابحث عن مسار المشروع
find / -name "frontend" -type d 2>/dev/null

# أو إذا كنت تعرف المسار
cd /var/www/company-docs/frontend
# أو
cd /home/username/project/frontend
```

### الخطوة 3: نسخ احتياطي (مهم جداً!)
```bash
# نسخ احتياطي قبل التعديل
cp env env.backup
```

### الخطوة 4: تعديل الملف
```bash
nano env
```

### الخطوة 5: تعديل المحتوى
```env
NEXT_PUBLIC_API_URL=http://93.127.160.182:5000/api/v1
NEXT_PUBLIC_APP_NAME=نظام إدارة الشركات
NEXT_PUBLIC_APP_VERSION=1.0.0
```

### الخطوة 6: حفظ الملف (في nano: Ctrl+O ثم Enter ثم Ctrl+X)

### الخطوة 7: إعادة تشغيل الفرونت اند
```bash
# إذا كان يعمل بـ PM2
pm2 restart frontend

# أو إذا كان يعمل بـ systemd
sudo systemctl restart frontend

# أو إذا كان يعمل مباشرة
# أوقف العملية (Ctrl+C) ثم:
npm run start
```

---

## 🔧 تعديل ملف env للباك اند

### نفس الخطوات لكن في مجلد backend:

```bash
ssh username@93.127.160.182
cd /path/to/project/backend

# نسخ احتياطي
cp .env .env.backup

# تعديل الملف
nano .env

# تعديل الإعدادات المطلوبة
# مثل:
# PORT=5000
# DATABASE_URL=...
# JWT_SECRET=...

# إعادة تشغيل الباك اند
pm2 restart backend
# أو
sudo systemctl restart backend
```

---

## ✅ التحقق من التعديلات

### عرض محتوى الملف:
```bash
cat env
# أو
cat .env
```

### التحقق من أن التغييرات تمت:
```bash
grep "NEXT_PUBLIC_API_URL" env
```

---

## 🛡️ نصائح أمنية مهمة

1. **نسخ احتياطي دائماً** قبل التعديل:
   ```bash
   cp env env.backup.$(date +%Y%m%d_%H%M%S)
   ```

2. **لا ترفع ملف env على Git**:
   ```bash
   # تأكد أن .gitignore يحتوي على:
   .env
   env
   *.env
   ```

3. **صلاحيات الملف**:
   ```bash
   # ملف env يجب أن يكون خاص (600)
   chmod 600 env
   chmod 600 .env
   ```

4. **استخدم متغيرات بيئة آمنة**:
   - لا تضع كلمات مرور في ملفات env على Git
   - استخدم secrets management في الإنتاج

---

## 🔍 العثور على مسار المشروع على الخادم

إذا كنت لا تعرف أين المشروع:

```bash
# البحث عن ملف env
find / -name "env" -type f 2>/dev/null | grep -v node_modules

# البحث عن مجلد frontend
find / -name "frontend" -type d 2>/dev/null

# البحث عن ملف package.json
find / -name "package.json" -type f 2>/dev/null | grep frontend

# عرض العمليات الجارية
ps aux | grep node
pm2 list
```

---

## 📱 استخدام محرر نصي عبر SFTP

يمكنك استخدام برامج مثل:
- **WinSCP** (Windows)
- **FileZilla** (جميع الأنظمة)
- **VS Code** مع إضافة Remote SSH

### مثال مع VS Code:
1. تثبيت إضافة "Remote - SSH"
2. الاتصال بالخادم: `F1` → `Remote-SSH: Connect to Host`
3. فتح مجلد المشروع
4. تعديل ملف env مباشرة

---

## 🚨 حل المشاكل الشائعة

### المشكلة: "Permission denied"
```bash
# استخدم sudo
sudo nano env

# أو غيّر الصلاحيات
sudo chown $USER:$USER env
chmod 644 env
```

### المشكلة: "File not found"
```bash
# تأكد من المسار
pwd
ls -la

# ابحث عن الملف
find . -name "env" -o -name ".env"
```

### المشكلة: التغييرات لا تعمل
```bash
# تأكد من إعادة تشغيل الخدمة
pm2 restart all

# أو أعد تشغيل Node.js
killall node
npm start
```

---

## 📞 مثال سريع (Copy & Paste)

```bash
# 1. الاتصال
ssh root@93.127.160.182

# 2. الانتقال للمجلد (غيّر المسار حسب موقعك)
cd /var/www/company-docs/frontend

# 3. نسخ احتياطي
cp env env.backup

# 4. تعديل الملف
nano env

# 5. في nano، عدّل السطر الأول إلى:
# NEXT_PUBLIC_API_URL=http://93.127.160.182:5000/api/v1

# 6. حفظ (Ctrl+O, Enter, Ctrl+X)

# 7. إعادة التشغيل
pm2 restart frontend
```

---

**✅ الآن أنت جاهز لتعديل ملف env على الخادم!**

