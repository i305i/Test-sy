# دليل إعداد OnlyOffice Document Server

## نظرة عامة

تم دمج OnlyOffice Document Server في النظام لتمكين المستخدمين من تحرير الملفات (Word, Excel, PowerPoint) مباشرة في المتصفح مع الحفظ التلقائي على السيرفر.

## الملفات المدعومة

- **Word**: `.docx`, `.doc`
- **Excel**: `.xlsx`, `.xls`
- **PowerPoint**: `.pptx`, `.ppt`
- **PDF**: `.pdf` (عرض فقط)
- **نص**: `.txt`, `.csv`

## المتطلبات

1. **OnlyOffice Document Server** - يجب تثبيته وتشغيله
2. **متغيرات البيئة** - يجب إعدادها بشكل صحيح

## إعداد OnlyOffice Document Server

### الطريقة 1: استخدام Docker (موصى به)

```bash
docker run -i -t -d -p 8080:80 --restart=always \
  -v /app/onlyoffice/DocumentServer/logs:/var/log/onlyoffice \
  -v /app/onlyoffice/DocumentServer/data:/var/www/onlyoffice/Data \
  -v /app/onlyoffice/DocumentServer/lib:/var/lib/onlyoffice \
  -v /app/onlyoffice/DocumentServer/db:/var/lib/postgresql \
  onlyoffice/documentserver
```

### الطريقة 2: التثبيت المباشر

راجع [دليل OnlyOffice الرسمي](https://helpcenter.onlyoffice.com/installation/docs-community-install-docker.aspx)

## إعداد متغيرات البيئة

### Backend (.env)

```env
# OnlyOffice Document Server
ONLYOFFICE_DOCUMENT_SERVER_URL=http://localhost:8080
ONLYOFFICE_SECRET=your-secret-key-change-in-production
BACKEND_URL=http://localhost:5000
```

### Frontend (.env.local)

```env
NEXT_PUBLIC_ONLYOFFICE_DOCUMENT_SERVER_URL=http://localhost:8080
```

**ملاحظة**: في الإنتاج، استبدل `localhost` بعنوان IP أو نطاق السيرفر.

📌 **للإعداد على السيرفر**: راجع ملف `ONLYOFFICE_SERVER_SETUP.md` للدليل الشامل.

## الميزات

### ✅ تحرير مباشر في المتصفح
- تحرير ملفات Word و Excel و PowerPoint مباشرة في المتصفح
- لا حاجة لتحميل الملفات على الجهاز

### ✅ حفظ تلقائي
- يتم حفظ التعديلات تلقائياً على السيرفر
- لا حاجة لرفع الملفات يدوياً بعد التعديل

### ✅ واجهة عربية
- واجهة المحرر باللغة العربية
- دعم كامل للكتابة من اليمين لليسار (RTL)

### ✅ إدارة الصلاحيات
- فقط المستخدمون الذين لديهم صلاحية التعديل يمكنهم تحرير الملفات
- دعم مستويات الصلاحيات: VIEW, EDIT, MANAGE

### ✅ أمان
- استخدام نظام Token آمن للوصول إلى الملفات
- التحقق من الصلاحيات قبل فتح المحرر

## كيفية الاستخدام

1. **فتح ملف للتحرير**:
   - انتقل إلى صفحة الملفات في الشركة
   - اضغط على زر "تحرير" (أيقونة القلم) بجانب الملف المدعوم
   - سيتم فتح المحرر في نافذة منبثقة

2. **التحرير**:
   - استخدم أدوات OnlyOffice لتحرير الملف
   - جميع التعديلات يتم حفظها تلقائياً

3. **الإغلاق**:
   - اضغط على زر "إغلاق" في أعلى المحرر
   - سيتم حفظ التعديلات تلقائياً قبل الإغلاق

## استكشاف الأخطاء

### المحرر لا يفتح

1. **تحقق من OnlyOffice Document Server**:
   ```bash
   curl http://localhost:8080/healthcheck
   ```
   يجب أن يعيد `true`

2. **تحقق من متغيرات البيئة**:
   - تأكد من أن `ONLYOFFICE_DOCUMENT_SERVER_URL` صحيح
   - تأكد من أن `BACKEND_URL` صحيح

3. **تحقق من CORS**:
   - تأكد من أن OnlyOffice Document Server يسمح بالطلبات من نطاقك

### الملف لا يُحفظ

1. **تحقق من Callback URL**:
   - تأكد من أن `BACKEND_URL` صحيح
   - Callback URL يجب أن يكون: `{BACKEND_URL}/api/v1/onlyoffice/callback`

2. **تحقق من الصلاحيات**:
   - تأكد من أن المستخدم لديه صلاحية التعديل
   - تحقق من سجلات Backend للأخطاء

### خطأ في تحميل الملف

1. **تحقق من Storage Service**:
   - تأكد من أن MinIO يعمل بشكل صحيح
   - تحقق من أن الملف موجود في Storage

2. **تحقق من Download Token**:
   - تحقق من أن Token لم ينتهِ صلاحيته
   - تحقق من أن Token لم يُستخدم مسبقاً

## الأمان

- جميع الطلبات إلى OnlyOffice محمية بـ JWT Token
- استخدام نظام Download Token للوصول الآمن إلى الملفات
- التحقق من الصلاحيات قبل فتح المحرر
- Callback URL محمي ضد الوصول غير المصرح به

## الأداء

- المحرر يتم تحميله بشكل ديناميكي فقط عند الحاجة
- الملفات يتم تحميلها عبر Download Token آمن
- الحفظ التلقائي يتم في الخلفية دون إزعاج المستخدم

## الدعم

للمزيد من المعلومات:
- [وثائق OnlyOffice](https://api.onlyoffice.com/)
- [دليل التكامل](https://api.onlyoffice.com/editors/basic)

