# 🏛️ معمارية النظام (System Architecture)

## نظرة عامة

نظام إدارة الشركات والوثائق مبني على معمارية حديثة قابلة للتوسع والصيانة.

---

## 📐 البنية العامة (High-Level Architecture)

```
┌─────────────────────────────────────────────────────────────┐
│                         Users / Clients                      │
│    (Web Browsers, Mobile Apps, Third-party Applications)    │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│                       Load Balancer / CDN                      │
│                      (Nginx / CloudFlare)                      │
└───────────────────────┬───────────────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼                               ▼
┌──────────────────┐           ┌────────────────────┐
│    Frontend      │           │   Backend API      │
│    (Next.js)     │◄──────────┤   (Node.js/       │
│                  │   REST API│    Express)        │
│  - React 18      │           │                    │
│  - TypeScript    │           │  - TypeScript      │
│  - Tailwind CSS  │           │  - Prisma ORM      │
│  - Zustand       │           │  - JWT Auth        │
│  - React Query   │           │  - WebSocket       │
└──────────────────┘           └─────────┬──────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
                    ▼                    ▼                    ▼
        ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
        │   PostgreSQL     │  │      Redis       │  │      MinIO       │
        │   (Database)     │  │     (Cache)      │  │  (Object Store)  │
        │                  │  │                  │  │                  │
        │ - User Data      │  │ - Sessions       │  │ - Documents      │
        │ - Companies      │  │ - Rate Limits    │  │ - Images         │
        │ - Documents Meta │  │ - Queues         │  │ - Files          │
        │ - Audit Logs     │  │ - Real-time      │  │                  │
        └──────────────────┘  └──────────────────┘  └──────────────────┘
```

---

## 🔧 البنية التفصيلية (Detailed Architecture)

### Frontend Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         Browser                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              Next.js App Router                    │    │
│  │                                                     │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────┐ │    │
│  │  │   Pages      │  │  Components  │  │  Hooks  │ │    │
│  │  │              │  │              │  │         │ │    │
│  │  │ - Dashboard  │  │ - UI         │  │ - Auth  │ │    │
│  │  │ - Companies  │  │ - Layout     │  │ - Data  │ │    │
│  │  │ - Documents  │  │ - Forms      │  │ - Utils │ │    │
│  │  │ - Users      │  │ - Tables     │  │         │ │    │
│  │  └──────────────┘  └──────────────┘  └─────────┘ │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────────┐ │    │
│  │  │          State Management (Zustand)          │ │    │
│  │  │                                               │ │    │
│  │  │  - Auth State    - UI State                  │ │    │
│  │  │  - User State    - Notifications State       │ │    │
│  │  └──────────────────────────────────────────────┘ │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────────┐ │    │
│  │  │        Data Fetching (React Query)           │ │    │
│  │  │                                               │ │    │
│  │  │  - Caching    - Invalidation                 │ │    │
│  │  │  - Prefetch   - Optimistic Updates           │ │    │
│  │  └──────────────────────────────────────────────┘ │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────────┐ │    │
│  │  │           API Client (Axios)                 │ │    │
│  │  │                                               │ │    │
│  │  │  - Request Interceptors                      │ │    │
│  │  │  - Response Interceptors                     │ │    │
│  │  │  - Error Handling                            │ │    │
│  │  │  - Retry Logic                               │ │    │
│  │  └──────────────────────────────────────────────┘ │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### Backend Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Backend Server (Node.js)                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │                 API Layer                          │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │         Express.js Router                │     │    │
│  │  │                                           │     │    │
│  │  │  /api/v1/auth      /api/v1/companies    │     │    │
│  │  │  /api/v1/users     /api/v1/documents    │     │    │
│  │  │  /api/v1/shares    /api/v1/reports      │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │            Middlewares                   │     │    │
│  │  │                                           │     │    │
│  │  │  - Auth (JWT)     - CORS                 │     │    │
│  │  │  - Validation     - Rate Limit           │     │    │
│  │  │  - Permissions    - Error Handler        │     │    │
│  │  │  - Upload         - Audit Logger         │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              Business Logic Layer                  │    │
│  │                                                     │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────┐ │    │
│  │  │ Controllers  │  │   Services   │  │  Utils  │ │    │
│  │  │              │  │              │  │         │ │    │
│  │  │ - Auth       │  │ - Auth       │  │ - JWT   │ │    │
│  │  │ - Companies  │  │ - Companies  │  │ - Hash  │ │    │
│  │  │ - Documents  │  │ - Documents  │  │ - File  │ │    │
│  │  │ - Users      │  │ - Storage    │  │ - Date  │ │    │
│  │  └──────────────┘  └──────────────┘  └─────────┘ │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │               Data Access Layer                    │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │          Prisma ORM                      │     │    │
│  │  │                                           │     │    │
│  │  │  - Type-safe queries                     │     │    │
│  │  │  - Migrations                            │     │    │
│  │  │  - Schema modeling                       │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │             Background Jobs Layer                  │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │           Bull Queue (Redis)             │     │    │
│  │  │                                           │     │    │
│  │  │  - Email Queue                           │     │    │
│  │  │  - Report Generation                     │     │    │
│  │  │  - OCR Processing                        │     │    │
│  │  │  - Expiry Notifications                  │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Data Flow

### تدفق البيانات: رفع وثيقة (Document Upload)

```
User Action
    │
    ▼
┌─────────────┐
│  Frontend   │
│             │
│ 1. Select   │
│    File     │
│ 2. Fill     │
│    Form     │
└──────┬──────┘
       │
       │ POST /api/companies/:id/documents
       │ multipart/form-data
       ▼
┌─────────────────────┐
│  Backend API        │
│                     │
│ 1. Auth Middleware  │──> Verify JWT Token
│    ✓                │
│                     │
│ 2. Upload MW        │──> Save to temp folder
│    ✓                │
│                     │
│ 3. Validation       │──> Check file type, size
│    ✓                │
│                     │
│ 4. Controller       │
│    ✓                │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Service Layer      │
│                     │
│ 1. Virus Scan       │──> ClamAV
│    ✓                │
│                     │
│ 2. Calculate Hash   │──> SHA-256
│    ✓                │
│                     │
│ 3. Upload to MinIO  │──> Object Storage
│    ✓                │
│                     │
│ 4. Save Metadata    │──> PostgreSQL
│    ✓                │
│                     │
│ 5. Update Company   │──> Recalculate completion
│    ✓                │
│                     │
│ 6. Create Audit Log │──> Audit trail
│    ✓                │
│                     │
│ 7. Send Notification│──> Redis Queue
│    ✓                │
└──────┬──────────────┘
       │
       │ Success Response
       │ { success: true, data: {...} }
       ▼
┌─────────────┐
│  Frontend   │
│             │
│ 1. Update   │
│    UI       │
│ 2. Show     │
│    Toast    │
│ 3. Refresh  │
│    List     │
└─────────────┘
```

### تدفق البيانات: المصادقة (Authentication)

```
User Login
    │
    ▼
┌─────────────┐
│  Frontend   │
│             │
│ Email       │
│ Password    │
└──────┬──────┘
       │
       │ POST /api/auth/login
       │ { email, password }
       ▼
┌─────────────────────┐
│  Backend API        │
│                     │
│ 1. Rate Limit Check │──> Redis (max 5 attempts)
│    ✓                │
│                     │
│ 2. Validate Input   │──> Zod Schema
│    ✓                │
│                     │
│ 3. Find User        │──> PostgreSQL
│    ✓                │
│                     │
│ 4. Compare Password │──> bcrypt
│    ✓                │
│                     │
│ 5. Generate Tokens  │
│    - Access Token   │──> JWT (15min)
│    - Refresh Token  │──> JWT (7days)
│    ✓                │
│                     │
│ 6. Save Session     │──> PostgreSQL + Redis
│    ✓                │
│                     │
│ 7. Update LastLogin │──> PostgreSQL
│    ✓                │
│                     │
│ 8. Audit Log        │──> PostgreSQL
│    ✓                │
└──────┬──────────────┘
       │
       │ Response
       │ {
       │   access_token,
       │   refresh_token,
       │   user: {...}
       │ }
       ▼
┌─────────────┐
│  Frontend   │
│             │
│ 1. Store    │──> localStorage/Cookie
│    Tokens   │
│ 2. Update   │──> Zustand State
│    State    │
│ 3. Redirect │──> Dashboard
│    to App   │
└─────────────┘
```

---

## 🔐 Security Architecture

### طبقات الأمان

```
┌─────────────────────────────────────────────────────────┐
│                    Layer 1: Network                      │
│                                                          │
│  - Firewall (UFW)                                       │
│  - DDoS Protection (CloudFlare)                         │
│  - SSL/TLS (Let's Encrypt)                              │
│  - IP Whitelisting (Optional)                           │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    Layer 2: Application                  │
│                                                          │
│  - CORS Configuration                                   │
│  - Rate Limiting (100 req/min)                          │
│  - CSRF Protection                                      │
│  - XSS Prevention                                       │
│  - Input Validation (Zod)                               │
│  - SQL Injection Prevention (Prisma)                    │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                Layer 3: Authentication                   │
│                                                          │
│  - JWT Tokens (HS256)                                   │
│  - Bcrypt Password Hashing (12 rounds)                  │
│  - 2FA/MFA (Optional)                                   │
│  - Session Management                                   │
│  - Token Rotation                                       │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                Layer 4: Authorization                    │
│                                                          │
│  - Role-Based Access Control (RBAC)                     │
│  - Permission Checks                                    │
│  - Resource Ownership                                   │
│  - Sharing Permissions                                  │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                  Layer 5: Data                           │
│                                                          │
│  - Database Encryption at Rest                          │
│  - SSL Connection to Database                           │
│  - MinIO Server-Side Encryption (AES-256)               │
│  - Presigned URLs (1 hour expiry)                       │
│  - Row-Level Security (RLS)                             │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                 Layer 6: Monitoring                      │
│                                                          │
│  - Audit Logs                                           │
│  - Security Event Logging                               │
│  - Intrusion Detection                                  │
│  - Anomaly Detection                                    │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Scalability Strategy

### Horizontal Scaling

```
                    ┌─────────────┐
                    │Load Balancer│
                    │   (Nginx)   │
                    └──────┬──────┘
                           │
          ┌────────────────┼────────────────┐
          │                │                │
          ▼                ▼                ▼
    ┌──────────┐     ┌──────────┐    ┌──────────┐
    │Backend #1│     │Backend #2│    │Backend #3│
    └────┬─────┘     └────┬─────┘    └────┬─────┘
         │                │                │
         └────────────────┼────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
    ┌─────────┐      ┌────────┐     ┌──────────┐
    │PostgreSQL│     │ Redis  │     │  MinIO   │
    │ Primary  │     │Cluster │     │ Cluster  │
    └────┬────┘      └────────┘     └──────────┘
         │
         ▼
    ┌─────────┐
    │PostgreSQL│
    │ Replicas │
    └─────────┘
```

### Caching Strategy

```
Request Flow:
    │
    ▼
┌─────────────┐
│Check Redis  │
│Cache        │
└──────┬──────┘
       │
       ├─── Cache Hit ───> Return from cache (Fast! 🚀)
       │
       └─── Cache Miss
              │
              ▼
       ┌──────────────┐
       │Query Database│
       └──────┬───────┘
              │
              ├──> Store in Redis (TTL: 1 hour)
              │
              └──> Return to client
```

### Caching Layers

```
Level 1: Browser Cache
    ↓
Level 2: CDN Cache (Static Assets)
    ↓
Level 3: Application Cache (Redis)
    - Sessions (TTL: 15 min)
    - User Data (TTL: 1 hour)
    - Company Lists (TTL: 5 min)
    ↓
Level 4: Database Query Cache
    ↓
Level 5: Database
```

---

## 📊 Monitoring & Observability

### Metrics Stack

```
┌──────────────────────────────────────────┐
│           Application Code               │
│                                          │
│  - Custom Metrics                        │
│  - Performance Timers                    │
│  - Error Tracking                        │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────┐
│          Prometheus (Metrics)              │
│                                            │
│  - CPU/Memory Usage                        │
│  - Request Rates                           │
│  - Response Times                          │
│  - Error Rates                             │
│  - Custom Business Metrics                 │
└────────────────┬───────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────┐
│           Grafana (Visualization)          │
│                                            │
│  - Dashboards                              │
│  - Alerts                                  │
│  - Reports                                 │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│          Winston (Logging)                 │
│                                            │
│  - Application Logs                        │
│  - Error Logs                              │
│  - Access Logs                             │
└────────────────┬───────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────┐
│       ELK Stack (Optional)                 │
│                                            │
│  - ElasticSearch (Storage)                 │
│  - Logstash (Processing)                   │
│  - Kibana (Visualization)                  │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│          Sentry (Error Tracking)           │
│                                            │
│  - Real-time Error Reporting               │
│  - Stack Traces                            │
│  - User Context                            │
│  - Release Tracking                        │
└────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

### Development Environment

```
Developer Machine
    │
    ├── Docker Compose
    │   ├── PostgreSQL (localhost:5432)
    │   ├── Redis (localhost:6379)
    │   ├── MinIO (localhost:9000)
    │   ├── Backend (localhost:5000)
    │   └── Frontend (localhost:3000)
    │
    └── Hot Reload Enabled
```

### Production Environment

```
                    ┌──────────────┐
                    │   CloudFlare │
                    │  (CDN + DDoS)│
                    └───────┬──────┘
                            │
                    ┌───────▼──────┐
                    │     Nginx    │
                    │(Load Balancer│
                    │   + SSL)     │
                    └───────┬──────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌────────────┐      ┌────────────┐     ┌────────────┐
│ Backend #1 │      │ Backend #2 │     │ Backend #3 │
│ (PM2/K8s)  │      │ (PM2/K8s)  │     │ (PM2/K8s)  │
└─────┬──────┘      └─────┬──────┘     └─────┬──────┘
      │                   │                   │
      └───────────────────┼───────────────────┘
                          │
      ┌───────────────────┼───────────────────┐
      │                   │                   │
      ▼                   ▼                   ▼
┌──────────┐       ┌──────────┐       ┌──────────┐
│PostgreSQL│       │  Redis   │       │  MinIO   │
│ Primary  │       │ Sentinel │       │ Cluster  │
│    +     │       │    +     │       │    +     │
│ Replicas │       │ Cluster  │       │ Replicas │
└──────────┘       └──────────┘       └──────────┘
```

---

## 📈 Performance Optimization

### Backend Optimization

```
- Database Query Optimization
  ├── Proper Indexing
  ├── Query Caching
  ├── Connection Pooling
  └── N+1 Query Prevention

- Caching Strategy
  ├── Redis for Sessions
  ├── Redis for Frequently Accessed Data
  └── Cache Invalidation Strategy

- Background Jobs
  ├── Email Sending (Async)
  ├── Report Generation (Async)
  ├── OCR Processing (Async)
  └── File Processing (Async)

- Code Optimization
  ├── Async/Await Properly
  ├── Avoid Blocking Operations
  └── Use Streams for Large Files
```

### Frontend Optimization

```
- Code Splitting
  ├── Dynamic Imports
  ├── Route-based Splitting
  └── Component Lazy Loading

- Asset Optimization
  ├── Image Optimization (Next/Image)
  ├── Font Optimization
  └── Bundle Size Reduction

- Rendering Optimization
  ├── Server-Side Rendering (SSR)
  ├── Static Site Generation (SSG)
  ├── Incremental Static Regeneration (ISR)
  └── Proper Memoization (useMemo, useCallback)

- Data Fetching
  ├── React Query Caching
  ├── Prefetching
  ├── Optimistic Updates
  └── Pagination/Infinite Scroll
```

---

## 🔄 CI/CD Pipeline

```
Developer
    │
    │ git push
    ▼
┌─────────────┐
│   GitHub    │
│ Repository  │
└──────┬──────┘
       │
       │ Webhook
       ▼
┌──────────────────┐
│ GitHub Actions   │
│                  │
│ 1. Checkout Code │
│ 2. Install Deps  │
│ 3. Lint Check    │
│ 4. Type Check    │
│ 5. Run Tests     │
│ 6. Build         │
└──────┬───────────┘
       │
       ├─── Tests Failed ───> Notify Developer
       │
       └─── Tests Passed
              │
              ▼
       ┌──────────────┐
       │ Build Docker │
       │    Images    │
       └──────┬───────┘
              │
              ▼
       ┌──────────────┐
       │ Push to      │
       │ Registry     │
       └──────┬───────┘
              │
              ▼
       ┌──────────────┐
       │ Deploy to    │
       │ Staging      │
       └──────┬───────┘
              │
              │ Manual Approval
              ▼
       ┌──────────────┐
       │ Deploy to    │
       │ Production   │
       └──────┬───────┘
              │
              ▼
       ┌──────────────┐
       │ Health Check │
       └──────┬───────┘
              │
              ├─── Failed ───> Rollback
              │
              └─── Success ───> Complete ✅
```

---

## 🎯 Design Principles

### SOLID Principles

- **S**ingle Responsibility: كل class/module له مسؤولية واحدة
- **O**pen/Closed: مفتوح للتوسع، مغلق للتعديل
- **L**iskov Substitution: يمكن استبدال الأصناف بأصناف فرعية
- **I**nterface Segregation: واجهات صغيرة ومحددة
- **D**ependency Inversion: الاعتماد على Abstractions

### Clean Architecture

```
Presentation Layer (UI)
        ↓
Application Layer (Business Logic)
        ↓
Domain Layer (Entities & Rules)
        ↓
Infrastructure Layer (DB, APIs, etc.)
```

---

## 📚 مراجع إضافية

- [المزيد من التفاصيل في DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)
- [توثيق الـ API في API_DOCUMENTATION.md](API_DOCUMENTATION.md)
- [دليل الأمان في SECURITY_GUIDE.md](SECURITY_GUIDE.md)

---

**آخر تحديث**: نوفمبر 2025

