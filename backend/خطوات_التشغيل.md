# 🚀 خطوات تشغيل Backend

## الخطوات بالترتيب:

### 1️⃣ تأكد من تشغيل PostgreSQL

```bash
# تشغيل PostgreSQL في Docker
docker run --name postgres-dev -e POSTGRES_PASSWORD=postgres123 -p 5432:5432 -d postgres:16

# أو تحقق إذا كان يعمل
docker ps
```

### 2️⃣ توليد Prisma Client

```bash
npx prisma generate
```

### 3️⃣ تشغيل Migrations

```bash
# سينشئ قاعدة البيانات والجداول
npx prisma migrate dev --name init
```

### 4️⃣ تعبئة البيانات التجريبية

```bash
npm run seed
```

### 5️⃣ تشغيل Backend

```bash
npm run start:dev
```

---

## ✅ البيانات الافتراضية بعد Seed

| الدور | Email | Password |
|-------|-------|----------|
| **Super Admin** | admin@companydocs.com | Admin@123 |
| **Supervisor** | supervisor@companydocs.com | Supervisor@123 |
| **Employee** | employee@companydocs.com | Employee@123 |

---

## 🧪 اختبار الـ API

بعد التشغيل، افتح:
- **Swagger UI**: http://localhost:5000/api-docs
- **Health Check**: http://localhost:5000/api/v1/health

---

## ⚠️ حل المشاكل الشائعة

### خطأ: "Can't reach database server"
```bash
# تأكد من تشغيل PostgreSQL
docker ps

# إذا لم يكن يعمل
docker start postgres-dev
```

### خطأ: "Error: P3009 - migrate.lock is missing"
```bash
# امسح المجلد وأعد المحاولة
rm -rf prisma/migrations
npx prisma migrate dev --name init
```

### خطأ في الـ Seed
```bash
# تأكد من تشغيل migrate أولاً
npx prisma migrate dev --name init
npm run seed
```

---

## 🎯 الأوامر المفيدة

```bash
npm run start:dev     # تشغيل في وضع التطوير
npm run build         # بناء للإنتاج
npm run studio        # فتح Prisma Studio (واجهة قاعدة البيانات)
npm run migrate       # تشغيل migration جديد
npm run seed          # تعبئة بيانات تجريبية
```

---

**✅ بعد إكمال هذه الخطوات، Backend سيعمل على:**
- API: http://localhost:5000/api/v1
- Swagger: http://localhost:5000/api-docs

